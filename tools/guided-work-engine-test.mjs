import fs from 'node:fs';
const d=fs.readFileSync('domain-intelligence.html','utf8');const g=fs.readFileSync('guided-work.html','utf8');const o=fs.readFileSync('core/casa-action-orchestrator.js','utf8');const checks=[['Start alle visible rule',/open\.length<2/.test(d)],['Batch start',/startBatch\(ids/.test(d)],['Guided page',/Godkend og fortsæt/.test(g)],['Direct work item',/Gennemgå forslaget/.test(g)],['Automatic next',/continueBatch\(\)/.test(d)],['Scroll return',/scrollIntoView/.test(d)],['Batch progress',/batchProgress/.test(o)],['Draft persistence',/guided-work-drafts/.test(g)]];for(const [n,ok] of checks){if(!ok)throw new Error(n)}console.log(`Guided Work Engine: ${checks.length} kontroller bestået.`);
