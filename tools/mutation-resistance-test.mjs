import fs from 'node:fs';import path from 'node:path';const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),'..');const source=fs.readFileSync(path.join(root,'release-governance.html'),'utf8');
function verify(s){const errors=[];if(!s.includes('id="capability-intelligence"'))errors.push('container');if(!s.includes('/core/casa-capability-intelligence.js'))errors.push('script');if(!s.includes('CasaCapabilityIntelligence.assess()'))errors.push('invoke');if(!s.includes('Capability Intelligence: v'))errors.push('render');if(!s.includes('Capability Intelligence kunne ikke indlæses'))errors.push('error');return errors}
const mutations=[['remove-script',source.replace(/<script src="\/core\/casa-capability-intelligence\.js[^>]*><\/script>/,'')],['remove-invoke',source.replace('CasaCapabilityIntelligence.assess()','CasaCapabilityIntelligence.missing()')],['remove-render',source.replace('Capability Intelligence: v','Capability result: v')],['remove-error',source.replace('Capability Intelligence kunne ikke indlæses','Capability unavailable')]];let n=0;if(verify(source).length)throw new Error('baseline integration invalid');for(const [name,s] of mutations){n++;if(verify(s).length===0)throw new Error(`mutation survived: ${name}`)}console.log(`Mutation resistance tests: ${n} mutations killed, 0 survived`);