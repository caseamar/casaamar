import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const registry=JSON.parse(fs.readFileSync(new URL('../registry/platform-journeys.json',import.meta.url)));assert.equal(registry.value_chain.length,9);assert.deepEqual(registry.value_chain,['idea','understanding','assessment','guidance','decision','execution','evidence','learning','documented-value']);assert.equal(registry.journeys.length,2);assert.deepEqual(registry.journeys[0].stages.map(x=>x.id),['demo','onboarding','implementation','go-live','hyper-care','maintenance']);
const store=new Map();const context={window:{dispatchEvent:()=>{},CasaEvents:{publish:()=>{}}},localStorage:{getItem:k=>store.get(k)||null,setItem:(k,v)=>store.set(k,v)},CustomEvent:class{constructor(type,init){this.type=type;this.detail=init.detail}},fetch:async()=>({ok:true,json:async()=>registry}),Date};vm.createContext(context);vm.runInContext(fs.readFileSync(new URL('../core/casa-platform-orchestrator.js',import.meta.url),'utf8'),context);const o=context.window.CasaPlatformOrchestrator;await o.load();assert.equal(o.current().stage.id,'onboarding');o.completeStage('baseline captured');assert.equal(o.current().stage.id,'implementation');assert.equal(o.nextAction().href,'/control/#next-best-action');o.start('continuous-improvement','observe');assert.equal(o.current().stage.id,'observe');o.recordDecision({id:'d1',outcome:'accepted'});assert.equal(o.state().decisions.length,1);console.log('Platform Orchestrator: 18 passed, 0 failed');
