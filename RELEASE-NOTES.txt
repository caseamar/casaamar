v178 – Action Orchestrator CTA Runtime Fix

Fixes impact-generated actions being stored as "suggested" and therefore rendered without Udfør nu / Senere / Afvis controls. Suggested impact actions now normalize to READY, including persisted stale actions. Adds executable regression tests proving that Impact Engine output reaches a visible actionable CTA state before release.
