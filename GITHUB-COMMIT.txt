fix(page-generator): declare generationMode in the correct function scope

Root cause
- generationMode was referenced by handlePageGenerate().
- It had accidentally been declared inside handlePageSectionGenerate().
- Full-page generation therefore failed with:
  "generationMode is not defined".

Fix
- Removed the misplaced declaration from the section generator.
- Declared generationMode inside handlePageGenerate().
- Enhanced /api/page-health to verify:
  - component-library.json can be loaded
  - OpenAI is configured
  - component contracts are available

Release
- v2026.07.24.58
- Worker 10.8-generation-mode-scope-fix
