feat(asset-upload): batched uploads, per-image context and visible pipeline status

- 20 MB is now clearly described as a per-file limit, not a total upload limit.
- Multiple eligible images are processed automatically in batches of five.
- Oversized or unsupported files remain visible, are skipped, and do not block other images.
- Upload selection shows total images, total size, batch count and files requiring action.
- Each image has its own optional description and season.
- Each image shows pipeline status:
  upload → Asset Object → AI analysis → relations → ready.
- Completed assets show GitHub path and Asset Object ID.
- Upload results remain visible after completion instead of disappearing.

Release: v2026.07.24.68
Worker: 11.8-batched-upload-status
