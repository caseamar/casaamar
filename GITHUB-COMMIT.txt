fix(platform): authoritative route and release synchronisation

- Worker internally maps extensionless admin URLs to the current HTML assets.
- No redirects are used.
- Added /api/platform-meta as authoritative deployed Worker metadata.
- Platform status is accepted only when manifest and Worker version/build match.
- Added visible mismatch warning instead of silently showing an old version.
- Versioned every platform-shell reference with this release.
- Added no-store headers for both /knowledge-center and /knowledge-center.html.

Release: v2026.07.24.86
Worker: 13.7-authoritative-route-sync
