feat(release): add schema-based compatibility and immutable release contracts

- Every release package now has release_schema and builder_version.
- Added release-contract.json with required fields and SHA-256 checksums.
- Pending packages are revalidated when Mission Control opens.
- Pre-flight runs before building and before accepting a GitHub commit.
- Legacy/incompatible packages are paused instead of silently processed.
- Old packages remain immutable and are preserved as superseded history.
- User can rebuild a compatible package from the current approved workspace.
- Platform updates only invalidate packages when release criteria/schema change.

Release: v2026.07.24.90
Worker: 14.1-release-compatibility-engine
Schema: 2.0
Build: 2026-07-27T00:23:36+02:00
