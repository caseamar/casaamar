fix(platform): remove redirect loop and add idle-aware version checks

- Removed _redirects and all URL rewriting.
- Automatic updates now use plain location.reload().
- Version polling runs only while the platform is visible and active.
- Polling pauses after 15 minutes of inactivity.
- Added “Fortsæt arbejdet” pause screen.
- Returning to the tab performs one immediate version check.
- Public website polling also stops when hidden or inactive.

Release: v2026.07.24.77
Worker: 12.7-idle-aware-refresh
