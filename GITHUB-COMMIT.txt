release: v2026.07.24.54 — single-pass website generation and API routing fix

Page Studio
- "Generér komplet hjemmeside-draft" and "Udfyld alle manglende felter"
  now use one complete-page API call instead of nine separate section calls.
- Added completion meter with section count and percentage.
- Added explicit diagnostics when Cloudflare returns HTML instead of JSON.
- Section-level regeneration remains available.

Worker
- Page API routes are matched before asset fallback.
- Full-page generator always returns every blueprint section.
- Component contracts enforce exact card/item counts.
- fill_missing mode preserves good existing copy and completes all missing
  fields in one coordinated response.
- complete mode writes the whole page as one coherent story.

Worker: 10.4-single-pass-page-generator
