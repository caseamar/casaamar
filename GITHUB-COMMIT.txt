fix(platform): restore full Mission Control and make auto-refresh route-safe

- Rolled back to the last known-good full Mission Control UI.
- Reimplemented automatic platform refresh without ambiguous extensionless reloads.
- Added explicit redirects from platform routes to their .html files.
- Automatic reload now always targets the explicit HTML asset.
- Preserved 30-second version checks, focus checks and visibility checks.
- Added no-cache rules and a dedicated regression test.

Release: v2026.07.24.76
Worker: 12.6-safe-auto-refresh
