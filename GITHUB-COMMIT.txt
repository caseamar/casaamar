fix(asset-upload): restore click and drag-and-drop initialization

Root cause
- Obsolete JavaScript still referenced removed buttons:
  registerAssets and importManifest.
- The resulting null.onclick error stopped the script before upload
  click and drag handlers were registered.

Fix
- Removed obsolete handlers.
- Added guarded initializeUploadManager().
- Added explicit click, keyboard and drag/drop event listeners.
- Added upload UI readiness status.
- Added hover, focus and drag-active feedback.
- Kept the existing direct GitHub upload, sync, analysis and relation flow.

Release: v2026.07.24.67
Worker: 11.7-upload-manager-runtime-fix
